{
  "name": "silvia-contact-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend del formulario de contacto de SilvIA (recibe el POST y envía el correo por SMTP).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "nodemailer": "^6.9.14"
  }
}
